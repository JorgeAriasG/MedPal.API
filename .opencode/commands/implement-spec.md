# Implement one accepted spec task

Read the active spec, plan, tasks, acceptance criteria, and linked ADRs. Confirm task ID, owning module, allowed paths, forbidden paths, and verification commands. Make surgical changes and run targeted tests before broad builds. Stop for an unapproved contract, tenant rule, provider choice, destructive migration, or production action.

